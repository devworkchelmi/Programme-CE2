import { Controller } from '@hotwired/stimulus';

/**
 * Contrôleur de démarrage — à remplacer par exercice_controller.js
 * (sélection QCM, désactivation du bouton si réponse vide, etc. — cf. architecture technique §2)
 * une fois les écrans enfant câblés en jour 7-10.
 */
export default class extends Controller {
    connect() {
        console.log('Carnet CE2 — Stimulus prêt.');
    }
}
